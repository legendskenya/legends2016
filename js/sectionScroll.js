$(document).ready(function(){
  $('#fullpage').fullpage({
    // Navigation
    anchors:['firstPage', 'secondPage', 'thirdPage'],
    slidesNavigation: true,
    // Scrolling
    css3: true,
    scrollingSpeed: 1500
    // Accessibility

    // Design

    // Custom Selectors

    // Events
  });
});
